import styled from 'styled-components';

import { inputStyle } from '@/styles/common';

export const SelectAuthorWrapper = styled.div``;

export const CustomSelect = styled.select`
  ${inputStyle};
`;

export const NoAuthorsMessage = styled.p`
  padding: 20px;
`;
